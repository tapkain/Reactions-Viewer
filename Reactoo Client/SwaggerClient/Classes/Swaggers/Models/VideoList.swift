//
// VideoList.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** an array of Video models */

open class VideoList: Codable {

    public var size: RequestListSize
    public var startKey: RequestListStartKey
    public var items: [Video]?
    public var users: [User]?


    
    public init(size: RequestListSize, startKey: RequestListStartKey, items: [Video]?, users: [User]?) {
        self.size = size
        self.startKey = startKey
        self.items = items
        self.users = users
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(size, forKey: "size")
        try container.encode(startKey, forKey: "startKey")
        try container.encodeIfPresent(items, forKey: "items")
        try container.encodeIfPresent(users, forKey: "users")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        size = try container.decode(RequestListSize.self, forKey: "size")
        startKey = try container.decode(RequestListStartKey.self, forKey: "startKey")
        items = try container.decodeIfPresent([Video].self, forKey: "items")
        users = try container.decodeIfPresent([User].self, forKey: "users")
    }
}

